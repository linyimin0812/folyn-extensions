{
  "name": "folyn-extension-diff-viewer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/diff": "^8.0.0",
    "esbuild": "^0.23.0",
    "folyn-extension-sdk": "^0.1.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "diff": "^9.0.0"
  }
}
