{
  "name": "folyn-extension-paste-history",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "^5.7.0"
  }
}
